const handleAuth = require("./handleAuth");
const tokenPaser = require("./tokenPaser");
const bodyParser = require("./bodyParser");
module.exports = {
    handleAuth,
    tokenPaser,
    bodyParser
};
